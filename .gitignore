# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# env
.env*.local
.vercel

# generated image-pipeline artifacts (regenerated by scripts/CI)
# NOTE: data/artworks.enriched.json IS tracked — the site reads it at build.
# NOTE: public/images/collection/ IS tracked — cached, rights-cleared webp
#       copies are committed so they deploy with the site.
/data/resolved-images.json
/data/image-candidates.json
/data/image-errors.json
/data/image-manifest.json
/reports/
